{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://netdust.be/flow.schema.json",
  "title": "netdust-flow definition",
  "type": "object",
  "required": ["flow", "version"],
  "additionalProperties": false,
  "allOf": [
    {
      "if":   { "not": { "required": ["extends"] } },
      "then": { "required": ["nodes", "edges"] }
    }
  ],
  "properties": {
    "flow":    { "type": "string", "pattern": "^[a-z][a-z0-9-]*$" },
    "version": { "type": "integer", "minimum": 0 },
    "state":   { "type": "object" },
    "extends": {
      "type": "string",
      "description": "Flow NAME (project pack first, then the runtime's roads) or path to derive from. Resolved by flow-lint at COMPILE time; the twin holds the complete graph and the walker never sees this key."
    },
    "remove": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Node ids to drop from the inherited graph, with every edge that touches them. Only meaningful with `extends`."
    },
    "nodes": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/node" }
    },
    "edges": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/edge" }
    }
  },
  "$defs": {
    "node": {
      "type": "object",
      "required": ["id", "kind"],
      "additionalProperties": false,
      "properties": {
        "id":    { "type": "string", "pattern": "^[a-z][a-z0-9-]*$" },
        "kind":  { "enum": ["agent", "gate", "human"] },
        "actor": { "type": "string", "minLength": 1 },
        "craft": { "type": "array", "items": { "type": "string" }, "minItems": 1 },
        "run":   { "type": "string" },
        "in":    { "type": "array", "items": { "type": "string" } },
        "out":   { "type": "array", "items": { "type": "string" } }
      },
      "allOf": [
        {
          "if":   { "properties": { "kind": { "const": "gate" } } },
          "then": { "required": ["run"] }
        },
        {
          "if":   { "properties": { "kind": { "const": "agent" } } },
          "then": { "required": ["craft"] }
        }
      ]
    },
    "edge": {
      "type": "object",
      "required": ["from", "to"],
      "additionalProperties": false,
      "properties": {
        "from": { "type": "string" },
        "to":   { "type": "string" },
        "when": {
          "type": "string",
          "description": "Machine-readable only: <state key> <op> <literal>; ops: == != > >= < <= in. Prose conditions are rejected by flow-lint (invariant I1)."
        }
      }
    }
  }
}
